# Sovariel 2025 — Official requirements (November 2025)
# Tested & working together on Python 3.11–3.12
# Updated for PyTorch-based quantum simulation and BCI integration

# Core numerical and scientific libraries
numpy==2.1.3              # Latest stable for array operations
scipy==1.14.1             # For additional scientific tools

# PyTorch ecosystem for quantum simulation
torch==2.5.0             # Latest stable with CUDA 12.x support
torchquantum==0.5.0      # Latest compatible with torch 2.5.0 (check PyPI for exact version)
# Note: Install with CUDA 12.x via --extra-index-url https://download.pytorch.org/whl/cu121
# Example: pip install torch==2.5.0 --extra-index-url https://download.pytorch.org/whl/cu121

# Audio and speech processing
sounddevice==0.5.1       # For real-time audio input
SpeechRecognition==3.11.0 # For voice command recognition

# Serial communication for haptic feedback
pyserial==3.5            # Stable version for serial devices

# Visualization
matplotlib==3.9.2        # For 2D plotting
pyqtgraph==0.13.7        # For real-time plotting (optional but included)

# BCI and physiological signal processing
neurokit2==0.2.9         # For EEG/BVP/EDA simulation and real device support

# System monitoring
psutil==6.1.0            # For memory profiling

# Web framework and server
Flask==3.1.0             # Lightweight web framework
gunicorn==23.0.0         # Production WSGI server for Docker

# Docker packaging (only needed locally or in CI, not inside container)
docker==7.1.0            # For local Docker image building

# Optional: for VR state export & 3D visualization
# open3d==0.18.0         # For 3D visualization (uncomment if needed)
# trimesh==4.0.10        # For mesh processing (uncomment if needed)
